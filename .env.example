# Client toggle to enable HF usage (set to true to attempt remote questions)
VITE_USE_HF=true

# (DO NOT PUT HF_API_KEY HERE) - HF API key must be set as a secret on Vercel or provided to vercel dev
# Example for local vercel dev run:
# HF_API_KEY=hf_xxx vercel dev

# Optional: use a specific HF model
HF_MODEL=google/flan-t5-small
